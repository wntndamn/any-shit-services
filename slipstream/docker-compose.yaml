services:
    slipstream:
        build: .
        container_name: slipstream
        env_file: .env
        ports:
            - "53:53/udp"
        volumes:
            - ./data:/data
        restart: unless-stopped
        cap_add:
            - NET_ADMIN
